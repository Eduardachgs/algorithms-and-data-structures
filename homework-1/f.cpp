#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, num, w;
    int max = 0;
    unordered_map<int, int> m;

    cin >> n;

    for (int i=0; i<n; i++) {
        cin >> num;
        if ()
    }

    if(s.size() > max) {
        max = s.size();
    }
    cout << max;
}