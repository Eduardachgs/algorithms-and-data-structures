#include <bits/stdc++.h>

using namespace std;

// O(1)

int main() {
    ios_base::sync_with_stdio(false);

    pair <int, int> p = {9, 0};
    pair<int, string> p2 = {8, "oi"};

    p.first = 4;
    p.second += 3;

    auto [f, s] = p;

    cin.tie(NULL);
}