#include <bits/stdc++.h>

using namespace std;

// versoes unordered, dados não ordenados, complexidade O(1) - O(N)

int main() {
    ios_base::sync_with_stdio(false);

    unordered_map<string, int> mapa;
    unordered_multiset<int> ms;
    unordered_set<int> s;

    cin.tie(NULL);
}